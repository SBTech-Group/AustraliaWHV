# ── Supabase (projeto PRÓPRIO do produto — hzaaqnbhhdpwfvtmuwkc) ──────────────
VITE_SUPABASE_URL=https://hzaaqnbhhdpwfvtmuwkc.supabase.co
VITE_SUPABASE_ANON_KEY=  # anon key do projeto novo (Dashboard > Settings > API)

# ── App ──────────────────────────────────────────────────────────────────────
VITE_PRODUCT_PRICE=R$ 49,90          # fallback exibido se o Hub cair (preço real vem do Hub)
VITE_PRODUCT_PRICE_NUM=49.90         # fallback numérico p/ o Brick (preço real vem do Hub)

# ── Mercado Pago (Checkout Transparente / Bricks — pagamento na própria tela) ─
VITE_MP_PUBLIC_KEY=APP_USR-5c6fac3f-ce9f-4fb3-8077-6b5db71d2a45  # Public Key PRODUÇÃO (pode ir no front)

# ── Supabase Secrets (setar via: supabase secrets set KEY=value NO PROJETO NOVO) ─
# Nunca usar prefixo VITE_ nestas variáveis — não devem ir para o frontend.
#
# SUPABASE_SERVICE_ROLE_KEY=...       (já disponível nas Edge Functions)
# SUPABASE_URL=...                    (já disponível nas Edge Functions)
#
# MP_ACCESS_TOKEN=...                 # Mercado Pago Access Token (autentica a re-consulta do pagamento)
# PRODUCT_PRICE=49.90                 # Preço em float para o MP (sem R$) — fallback se o Hub cair
# PRODUCT_CICLO=anual                 # ciclo da assinatura (anual|semestral|trimestral|mensal) — fallback
# APP_URL=https://australia-whv.com   # URL do app (back_urls + mensagem de boas-vindas)
#
# RESEND_API_KEY=re_...               # e-mail de boas-vindas (sem isto, só WhatsApp)
# EMAIL_FROM=Monitor WHV <noreply@sbtech-group.com>   # remetente (domínio verificado no Resend)
#
# EVOLUTION_API_URL=...               # Instância Evolution PRÓPRIA do produto (ver nota de ban/escala)
# EVOLUTION_API_KEY=...
#
# AUSTRALIA_SAAS_CRON_SECRET=...       # protege x-cron-secret da Edge Function australia-monitor
# ADMIN_EMAILS=voce@sbtech-group.com   # opcional — restringe quem pode usar /admin (allowlist, csv)

# ── Integração com Hub SB Tech (admin-hlg.sbtech-group.com) — modelo SaaS ─────
# Produto cadastrado no Hub com slug 'australiawhv' e modelo='saas'.
# Ao pagar, o webhook chama hub-register-account → cria cliente+assinatura+saas_conta.
# HUB_FUNCTIONS_URL=https://<hub-ref>.supabase.co/functions/v1
# HUB_PRODUCT_SLUG=australiawhv        # slug do produto no Hub (usado por hub-public-plans)
# HUB_PROVISIONING_TOKEN=...           # MESMO valor que AUSTRALIAWHV_PROVISIONING_TOKEN no Hub.
#                                      # A Edge Function australia-access REUSA este token como
#                                      # segredo compartilhado de entrada (Authorization: Bearer)
#                                      # para o webhook de vencimento vindo do Hub (op warn/cut/extend).
